var scene,camera,renderer;function init(){scene=new THREE.Scene;var e=window.innerWidth/window.innerHeight;(camera=new THREE.OrthographicCamera(-75*e,75*e,75,-75,1,1e3)).position.set(200,200,200),camera.lookAt(scene.position),(renderer=new THREE.WebGLRenderer({antialias:!0})).setSize(window.innerWidth,window.innerHeight),document.getElementById("container").appendChild(renderer.domElement);var n=new THREE.LineBasicMaterial({color:16777215}),r=new THREE.BoxGeometry(100,100,100),i=new THREE.EdgesGeometry(r),t=new THREE.LineSegments(i,n);t.position.set(0,0,0),scene.add(t)}function animate(){requestAnimationFrame(animate),scene.children[0].rotation.x+=.01,scene.children[0].rotation.y+=.01,renderer.render(scene,camera)}function onWindowResize(){var e=document.getElementById("container"),n=e.clientWidth,r=e.clientHeight;renderer.setSize(n,r);var i=n/r;camera.left=-100*i,camera.right=100*i,camera.top=100,camera.bottom=-100,camera.updateProjectionMatrix(),renderer.render(scene,camera)}window.addEventListener("DOMContentLoaded",(()=>{init(),animate(),onWindowResize()}));